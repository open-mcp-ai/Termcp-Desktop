{
  "name": "termcp-gui",
  "version": "0.1.0",
  "private": true,
  "description": "Wails desktop control plane and SSH workspace for termcp",
  "scripts": {
    "dev": "npm --prefix frontend run build && node scripts/serve.mjs",
    "dev:wails": "go run github.com/wailsapp/wails/v2/cmd/wails@v2.15.0 dev",
    "build": "npm --prefix frontend run build && go run github.com/wailsapp/wails/v2/cmd/wails@v2.15.0 build",
    "check": "npm --prefix frontend run check && npm --prefix frontend run build && go test ./..."
  }
}
