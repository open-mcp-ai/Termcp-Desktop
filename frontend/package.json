{
  "name": "termcp-desktop-frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "check": "node --check src/app.js",
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {"vite": "^7.1.7"}
}
